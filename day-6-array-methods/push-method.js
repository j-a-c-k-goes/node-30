// Push Method in Arrays is used to insert an elment @ the array's caboose
let testArray = new Array(3);
testArray = ["a","b", "c"];
const combined = testArray.join();
testArray.push(combined)
console.table(testArray);