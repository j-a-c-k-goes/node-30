// Shifts removes the first element in an array
const test = ["Lighter", "CD-Rom", "Ruler", "Mug"];
console.log("Array length:", test.length);
console.table(test);
test.shift();
console.log("Array Length:", test.length);
console.table(test);